<% unless @book.errors.empty? %>
  $(".admin-book-form .pull-right").before("<%= escape_javascript(parse_errors(@book.errors)) %>");
<% else %>
  $("main").html("<%= escape_javascript(render 'list') %>");
<% end %>